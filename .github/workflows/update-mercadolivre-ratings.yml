name: Update Mercado Livre Ratings (Weekly)

on:
  schedule:
    # 🕒 Domingo às 03:00 UTC (00:00 horário de Brasília)
    - cron: "0 3 * * 0"

  # Permite rodar manualmente pelo GitHub (botão "Run workflow")
  workflow_dispatch:

jobs:
  update-ratings:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 📦 Install dependencies
        run: npm install

      - name: ⚙️ Generate Prisma Client
        run: npx prisma generate

      - name: ⭐ Update Mercado Livre ratings (HTML-only)
        run: node --loader ts-node/esm scripts/updateMercadoLivreRatings.ts
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
